# Project Behavioral Rules

- **Deployment Control**: DO NOT deploy to production (i.e. do not run `git push origin main` or trigger production workflows) under any circumstances unless the user explicitly commands you to do so. Local commits and branches are allowed, but remote pushes are blocked until explicit approval.
